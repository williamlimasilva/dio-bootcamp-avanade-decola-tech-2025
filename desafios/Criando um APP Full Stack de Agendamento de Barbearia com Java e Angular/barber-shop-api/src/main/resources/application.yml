spring:
  application:
    name: barber-shop-api
  datasource:
    url: ${DB_URL:jdbc:postgresql://db:5432/barber-shop-api}
    username: ${DB_USER:barber-shop-api}
    password: ${DB_PASSWORD:barber-shop-api}
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: validate
